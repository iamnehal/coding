

class Solution
{   
    public:  
    //Function to find transpose of a matrix.
    void transpose(vector<vector<int> >& a, int n)
    {   int temp=0;
    for(int i=0;i<n;i++)
    {
        for(int j=i;j<n;j++)
    {
    if(i!=j)
    { temp=a[i][j];
    a[i][j]=a[j][i];
    a[j][i]=temp;
        
    }
    }
    } 
        return ;
    }  
